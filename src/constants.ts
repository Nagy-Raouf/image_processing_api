import path from 'path';

// resolve path for full image
export const fullImagePath = path.resolve(__dirname, '../images/');

// resolve path for cached image
export const cachedImagePath = path.resolve(__dirname, '../images/thumb/');
